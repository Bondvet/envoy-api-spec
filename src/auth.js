/**
 * @apiDefine Auth
 * @apiHeader {String} authorization API-key, prefixed with `Bearer `.
 * so f.e. if your API key is `MY_API_KEY`, the value of the `Authorization` header
 * needs to be `Bearer MY_API_KEY`
 */

